# Raster Renderer

This is a simple 3D viewer! It's a bit buggy, it can only read .obj files, and frustum culling is on the way.

It relies on SDL3 for rendering, and glam for the vector math. To change the file it's viewing, replace the string near the top of `main.rs`.
